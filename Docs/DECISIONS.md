# Decisions Log

Elke architectuur-, ontwerp- of balans-beslissing hier in 1-2 regels bijschrijven, met datum. Dit is het geheugen van het project — de AI-agent leest dit om consistent te blijven over sessies heen.

> Formaat: `- [JJJJ-MM-DD] Beslissing — korte reden.`

## Vastgelegd (uit de brief, v2)

- [2026-06-12] **Roadmap vernieuwd na volledige code-audit** — het A–Z plan in de brief is vervangen door `Docs/ROADMAP.md` (levend document). Volgorde: (1) beach-map = echte wereld (homes-registry → koopbare woningen → NPC-leven → winkels/leveringen → navmesh → end-to-end), (2) 1-50 écht goed (balans: late Cali-seed-ROI kapot, XP-uit-verkoop te laag, level 32 leeg; mid-game dag-orders/bulk-deals; co-op-fixes: toasts alleen naar host in `HeatComponent.cpp:137`, deuren/liften bewust `bReplicates=false` → in co-op testen; save-gat: waterfles-vulling), (3) 50+ shop-fase gereserveerd. Doorlopend: Variant_Horror/Shooter verwijderen, packaged build maken (nodig voor LAN co-op).
- [2026-06-02] **Game modes bij New Game (Normal/Sandbox/Testing) + gravity/zwerf-fixes**. New Game vraagt nu na de slot-keuze een **modus** (MainMenu MenuMode 3): **Normal** (kaal), **Sandbox** (EUR 1.000.000 cash+bank + volledige starter-kit, alle zaden), **Testing** (EUR 5.000 cash+bank + starter-items: soil/water/papers/bags/jars/pot/droogrek/packbench + 2 zaden). `EGameStartMode` in SaveGameSubsystem; `RequestNewGame(slot, mode)` onthoudt de modus, `RunPendingOnWorldReady` → `ApplyStartMode` geeft de host-pawn geld+items na de level-reload. **Player-fixes**: wereld-zwaartekracht werd op BeginPlay (en in stuck-recovery) op -980 gezet als de map ~0 had (jump→eeuwig zweven), GravityScale/JumpZVelocity runtime geforceerd (BP-default overschreef constructor), muis-lock geforceerd bij gameplay-start na reload, en het anti-stuck-vangnet teleporteert naar een getracede vloer.
- [2026-06-02] **Heat-fix: geen passieve opbouw meer**. `UHeatComponent` liet heat **'s nachts vanzelf oplopen** (`NightHeatPerSecond`), dus stil thuis zitten 's nachts → heat naar ~100% → elke 5s kans op bust/overval (je werd beroofd zonder iets te doen). Nu: heat **zakt altijd** (dag 0.8/s, nacht 0.3/s) en stijgt **alleen** via `AddHeat` (straat-dealen). Risico-events alleen 's nachts bij heat ≥ 80, zeldzamer (elke 12s, basiskans 12%, bust = 40% daarvan). Rustig blijven = heat → 0 = geen events.
- [2026-06-02] **Centrale toast-meldingen i.p.v. debug-tekst**. Nieuwe `UWeedToast` (z-order 48, midden-boven, stapelt korte pillen die in-/uitfaden). `UWeedToast::Notify(Key, Time, FColor, Msg)` heeft **exact dezelfde signatuur** als `GEngine->AddOnScreenDebugMessage`, zodat alle ~90 call-sites in 16 bestanden mechanisch zijn omgezet (script-replace + `#include "UI/WeedToast.h"`). Zelfde-key updates in-place, dubbele tekst stapelt niet, max 5 tegelijk. Fallback naar de engine-melding als de widget er (nog) niet is. Geregistreerd per lokale speler in `EnsureWidget`. Plant-model is intussen ook verbeterd (steel + brede fan-leaf-bladeren in kransen + duidelijke donkerpaarse colas bij rijpheid); hand-info-popup staat links-midden.
- [2026-06-02] **Fix shop-categorieën + Grow/Supplies-split**. **Bug**: `SetSupplierCat` klemde op `SupplierCatCount-1` (=7), waardoor de nieuwe categorieën **8 (Pot Upgrades)** en **9 (Plant care)** terugvielen op **7 (Furniture)** → furniture in de grow shop, en de laatste 2 tabs wisselden niet. Fix: clamp nu `0..9` (SupplierCatCount blijft 8 vanwege de legacy WeedShopHUD-array). Tabbalk is nu een **UWrapBox** (auto-breedte, wrapt naar 2e rij → geen overlappende labels meer). Categorieën **logisch herverdeeld**: **Grow shop** = Seeds, Pots, Soil, Water, Pot Upgrades, Care (alles om te kweken); **Supplies** = Papers, Drying, Packing, Furniture (verwerken/verkopen/inrichten). Labels ingekort (Upgrades/Care).
- [2026-06-02] **In-game lamp-editor (hoofdmenu) + zieke-plant-markers**. (1) `AGrowPlant` toont een zwevend bolletje per besmette plek: **wit = mold, oranje = pest** (engine-sphere + BasicShapeMaterial dynamic kleur, los van het plant-materiaal, repliceert via OnRep_Slots). (2) **Lamp-editor** in `UMainMenuWidget`: knop "Edit lamps" rechtsonder zet een edit-modus aan; sleep met de muis genummerde markers (per neon-gloed) naar hun lamp op de achtergrondfoto. Posities worden in **GConfig** (`[ThePlugSIM.MenuGlows] Glow<i>=fx,fy`) bewaard en bij het opbouwen geladen (override van de hardcoded defaults), dus geen editor nodig en het blijft bewaard. Drag via globale muis-positie/viewport (geen per-handle delegates); auto-save bij loslaten.
- [2026-06-02] **Relatie/contact/afspraak-progressie rework**. (1) Een goede joint maakt **niet** meer meteen een vaste klant: hij bouwt vooral de relatie op. Of 'ie nú grammen vraagt wordt **elke keer** opnieuw bepaald door z'n stats — alleen als 'ie al **koper** is (prospect→koper via `RefreshProspect`, verslaving ≥ `AddictionToBuy`) **én** niet op cooldown. Anders "sample given" met voortgang-hint (verslaving x/AddictionToBuy, respect x/UnlockRespect). (2) **Nummer/contact is nu RESPECT-gedreven** (`UnlockRespect=45`, was loyaliteit) + de joint-feedback toont de respect-voortgang naar het nummer. (3) **Telefoon-afspraken schalen met progressie**: `SendRandomAppointment` kiest alleen ontgrendelde contacten die niet op cooldown zijn én onder de **dag-cap** (`MaxApptsPerDay=2`), gewogen + met een globale send-kans die met de "warmte" (verslaving+loyaliteit+respect) meeschaalt — begin zelden, later vaker, ~1-2 deals/dag/NPC. `FNpcState` heeft nu `ApptDay/ApptCountToday`; `NoteAppointment`/`CanAppointToday`/`CurrentDay`/`IsUnlocked` toegevoegd.
- [2026-06-02] **Hand-info popup (rechts-midden)**. Nieuwe `UHandInfoWidget` (z-order 6, boven de hotbar) toont zachtjes (fade in/uit) wát je in je hand hebt: een **type-tag** met kleur (WET BUD / DRIED BUD / BAGGIE / JOINT / SEED / SOIL / WATER BOTTLE / EMPTY CONTAINER / SPRAY / FERTILIZER / PAPERS / PLANT POT / PLACEABLE), de **naam**, en de **relevante stats** (gram + THC% + Q% voor wiet, x-aantal + gram-elk + THC% voor joints, "holds up to Ng" voor lege containers, anders x-aantal). Leest het actieve item uit de lokale inventory; verbergt bij vol-scherm UI (telefoon-app/pauze/titel/settings). Lost op dat de hotbar te weinig ruimte had om duidelijk te tonen of iets een Bag/Seed/etc. is.
- [2026-06-02] **Packing Station herbouwd: kies bag → strain → gram-slider**. Een "bag" is `Bag_<strain>` met **quantity = totaal gram** (per gram geprijsd), dus de container-capaciteit is gewoon een **MAX** (zoals papers bij joints). Nieuwe `UPackWidget`-flow: (1) kies gedroogde strain, (2) kies bag/jar, (3) **gram-slider 1..max** + "Pack Xg"-knop (slider live uitgelezen in NativeTick, label updatet zonder rebuild). Nieuwe RPC `RequestPackGrams/ServerPackGrams(BudId, ContId, Grams)` verpakt exact het gekozen aantal (geklemd op cap én beschikbare wiet) en verbruikt 1 container. **Container-capaciteiten realistischer**: Bag 2/5g, **kleine jar 14g**, **grote jar 50g**, persblok 100g, vuilniszak 500g; betere level-progressie (Jar15 lvl7, Block lvl11). Oude `ServerPack` (vaste cap-batch) blijft bestaan maar de UI gebruikt de slider-versie.
- [2026-06-02] **Klant-deals: joint-naar-verkoop + per-NPC cooldown**. (1) Geef je iemand een **goede** gratis joint (niet te zwak), dan wordt 'ie meteen koper (`ACustomerBase::BecomeBuyerNow`) en **vraagt 'ie grammen van een verkoopbare strain die jij nu in voorraad hebt** (Bag_ heeft voorrang, anders Bud_) zodat je direct kunt dealen. (2) **Persistente per-NPC cooldown** in `UNpcRegistryComponent` (`FNpcState.LastDealAbs`, monotone dag*1e6+tijd): na élke deal — in persoon **of** telefoon — wordt `MarkDealt(NpcId)` aangeroepen (in `ACustomerBase::SubmitOfferProduct`, geldt ook voor afspraak-klanten). `AssignNpc` slaat NPC's op cooldown over; `ContactsComponent::SendRandomAppointment` slaat ze ook over (geen nieuw bericht als iedereen op cooldown is). `DealCooldownSeconds=240`. (3) Staande klant her-bestelt pas na `OrderCooldownSeconds` (60→**240**). Bedoeld voor straks ~100 NPC's: dezelfde persoon komt niet snel opnieuw halen.
- [2026-06-02] **Mold/pest-systeem + spray + fertilizer + langzamere over-rijp-dood**. (1) **Over-rijp**: oogstklare planten gingen veel te snel dood (verval × groeisnelheid). Nu vast in ECHTE tijd: ~90% over ~4 min, laatste 10% over ~3 min → ruim tijd om te oogsten. (2) **Mold/pest** per plek (`SlotAfflict` 0/1/2 + `SlotAfflictTime`, gerepliceerd): vanaf **crew-level 12** kleine kans per seconde, **schaalt met (1−zorg-kwaliteit)** (slechter verzorgd = grotere kans). Een besmette plant **stopt met groeien**; **3 min** curable, daarna ~2,5 min langzaam dood (zaad weg). Kan tijdens groei én als 'ie klaar is; zieke klare planten kun je niet oogsten tot je sprayt. (3) **Sprays** (`Spray_Fungicide`/`Spray_Pesticide`/`Spray_Broad`, level 10/10/16) genezen mold/pest/beide — gebruiken = item in hand + LMB op de pot. (4) **Fertilizer** (`Fertilizer_Basic` +15%, `Fertilizer_Bloom` +30%, level 10/14) = opbrengst-boost voor één oogst (`FertYieldMult`, reset na oogst). (5) Nieuwe Grow-shop tab **"Plant care" (cat 9)** voor sprays/mest. De pot-prompt toont besmetting + resterende tijd; alles bewust **pas vanaf ~level 10-15** zodat het begin niet frustreert.
- [2026-06-02] **Pot-upgrades uitgebreid + "Pot Upgrades"-tab in de Grow shop**. Pot-upgrades (per geplaatste `AGrowPlant`, bits in `PotUpgradeMask`) zijn uitgebreid van 3 naar 6: 0 Drainage (+10% kwaliteit), 1 Insulation (water lekt 0.5×), 2 Bloom (+20% yield), **3 Grow tent** (+15% kwaliteitsplafond via `GetMaxCare`), **4 LED grow lamp** (+30% groeisnelheid in Tick), **5 Auto-watering** (houdt `WaterLevel`=1, geen handmatig water). `FPotUpgradeDef` heeft nu `MinPotTierIndex` (tent/lamp vanaf Clay, auto-water vanaf Plastic); `IsPotUpgradeAllowed`/`GetPotTierIndex` gates dit, ook server-side in `ServerBuyPotUpgrade`. Nieuwe Grow-shop categorie **8 "Pot Upgrades"** (`AppCats={0,1,2,3,8}`): `UPhoneWidget::FillPotUpgradesInto` lijst alle geplaatste potten en koopt per pot via `UPhoneClientComponent::RequestPotUpgradeFor(Pot, idx)` → bestaande `ServerBuyPotUpgrade` (bankgeld). De telefoon-**Upgrades-app** (DT_Upgrades) blijft voor algemene/apartment-upgrades (GrowGear/Storage/Security/Premises). Het oude per-pot HUD-paneel (interact op pot) blijft ook werken.
- [2026-06-02] **Level-gating op álle shop-items + locked onderaan**. `UStoreComponent::RequiredLevelFor` is nu een **member** (geen static) en geeft **elk** item een vereist level (basis/goedkoop = 1, betere tiers hoger): potten 1/2/5/12, aarde 1/4/9, water 1/4/8/14, papers 1/3/6/10, droogrek 2/6/13, verpaktafel 1/7/15, containers 1/2/4/5/9/16, meubels matras1/tafel2/kist3/schap5/koelkast6. **Seeds** schalen mee met THC% (1/3/6/10/15). `GetSupplierCategory` sorteert nu elke categorie op **vereist level oplopend, dan prijs** → lage/goedkope items bovenaan en alle **locked items altijd onderaan gegroepeerd** (nooit tussen unlocked items). De **Upgrades-app** sorteert: koopbaar → owned → locked (verkeerde fase) onderaan. Server-buy-check en winkel-UI gebruiken nu de member-versie.
- [2026-06-01] **Droogrek-UI + interactie + composiet-meshes**. (1) Droogrek opent nu een **scherm** (`UDryingRackWidget`, z-order 32) i.p.v. de oude hand-item-interactie — net als de verpaktafel, en **ook met lege hotbar**. Links: drogende/klare batches met **progress-bar + resterende tijd** (klaar = groen + Collect-knop, plus "Collect all ready"); rechts: je natte wiet uit de inventory met **Hang**-knop. `ADryingRack::Entries` repliceert nu (clients zien progress). Server-acties via `UPhoneClientComponent::ServerDryHang/Collect/CollectAll` (afstand-check). Bedraad in `AThePlugSIMCharacter` (LMB + E), net als shelf/packbench. (2) **Home-knop op het homescreen sluit de telefoon** (anders -> terug naar homescreen). (3) **Composiet-meshes** via `Placement/PropMeshKit.h` (`MakeDeco` = absolute-scale deco-wortel zodat onderdelen in echte cm los van de niet-uniforme root-schaal staan; `MakePart`/`SetPart`): verpaktafel (blad+poten+accenten), schap (zijpanelen+4 planken) / kist (romp+schuin deksel+beslag), droogrek (frame+gaas+5 dwarsstangen), en `APlaceableProp` Table/Fridge/Mattress. De originele root-kubus blijft de **collision/footprint** (onzichtbaar gemaakt) zodat plaatsing/saves niet wijzigen.
- [2026-06-01] **Save-picker v3.3 — autosave kiesbaar + schonere slots**. In Load toont een slot met **zowel** een handmatige save als een autosave een **extra autosave-knop** (laad beide los; `LoadSlotSpecific`/`GetSlotDetailsEx`/`HasAutoSaveInSlot`). De **"SLOT n"-tekst vervalt** zodra er een save in het slot zit (alleen lege slots tonen 'm). Slot-saldo wordt **per bestand** gelezen (handmatig vs autosave) zodat het klopt met cash+bank. Slot-rijen worden nu dynamisch opgebouwd in een `SlotsBox`.
- [2026-06-01] **Save-systeem v3.2 — speler-positie + autosave los van handmatige save**. (1) Save bewaart nu **waar de speler staat + kijkrichting** (`FPlayerSaveData.bHasTransform/Location/Rotation`); bij Load wordt de pawn echt **teruggezet naar het save-punt** (`TeleportTo` + `SetControlRotation`). Hiermee voelt "Load" als een echte reload i.p.v. doorgaan. (2) **Autosave schrijft naar een apart bestand** (`<slot>_auto`), de handmatige Save naar `<slot>`. Een echte **Load** (`LoadSlot`/pauze-Load) herstelt **altijd je laatste handmatige save** (je echte save-punt) — de autosave overschrijft dat punt dus niet meer. **Continue** (`QuickContinue`) pakt het **nieuwste** van handmatig vs autosave (resume waar je was), via `FDateTime SavedAt` in de save. **New Game** in een slot **wist beide** bestanden (geen stale save). Reden: user wilde dat Load écht terug gaat naar het moment dat de save gemaakt is, niet de telkens-overschreven autosave. (3) Gerepliceerde `LoadCounter` op de GameState + "Loading…/Loaded"-melding rechtsboven (zoals de save-indicator), zodat iedereen ziet dat er herladen is.
- [2026-06-01] **Save-systeem v3 — wereld-objecten persistent**. Save/Load serialiseren nu álle geplaatste placeables en spawnen ze bij load opnieuw met hun staat: **potten/planten** (`AGrowPlant::CaptureState/RestoreState` — pot-tier, upgrades, soil, care/water, en elke plek: strain+groei+fase), **shelves/chests** (`AStorageShelf` tier + volledige inhoud = de thuis-stash), **droogrekken** (`ADryingRack` tier + drogende batches via `GetEntries/RestoreEntries`), **verpaktafels/meubels/props/ATM** (tier/id + transform). `UWeedShopSaveGame.Placed` (TArray<FPlacedObjectSave>). Load **vervangt** de huidige placeables door de opgeslagen set (alleen als de save echt objecten bevat, zodat verse/legacy saves de map-default houden). Hiermee is de eerder genoteerde "nog niet in de save"-laag afgerond.
- [2026-06-01] **Save-systeem v2 (co-op-bewust, op username)** (user-gevraagd). `UWeedShopSaveGame` slaat nu op: **gedeeld** (dag/tijd, milestone totaal+fase, gekochte upgrades) + **per-speler op username** (`FPlayerSaveData`: cash, bank, telefoon-upgrade, volledige inventory incl. THC/kwaliteit). **Alleen de host schrijft** (autoriteit). `SaveGame` merge't: verbonden spelers worden ge-upsert, eerder-opgeslagen (offline) spelers blijven bewaard. **Host autosavet** elke `AWeedShopGameState::AutoSaveSeconds` (default 180s, timer) + manual save neemt alle verbonden spelers mee. **Co-op client** kan een host-save triggeren via het pauze-menu (`UPhoneClientComponent::RequestSaveGame`→`ServerRequestSave`). `LoadGame` herstelt gedeeld + alle nu-verbonden spelers; een later joinende speler wordt **op username** automatisch hersteld in `AThePlugSIMCharacter::BeginPlay` (`RestorePlayerByPawn`, 1× per sessie). Legacy v1-saves (alleen host-cash/bank) gaan naar de host. Helpers: `InventoryComponent::ClearAll`, `Milestone::RestoreState`, `Upgrade::RestorePurchased/GetPurchasedIds`. **Nog niet in de save:** geplaatste wereld-objecten (potten/planten/shelves/meubels + shelf-inhoud) — volgende save-milestone.
- [2026-06-01] **Verkoop-flow = realistisch, NOOIT direct van het schap** (user-bevestigd). Shelves/`AStorageShelf` zijn **puur opslag/stash** (zien hoeveel je thuis hebt). Om te verkopen moet de waar fysiek **uit de pot/opslag → in je inventory → naar de packing-bench → van jar naar baggies**, en dan verkoop je vanuit je **inventory** (Sell-app of deal met NPC op straat). Potten in opslag zijn voor **losse/bulk-verkoop** (iemand wil een hele pot of je maakt er een kleinere stash van). Verkochte/over te dragen waar zit **altijd in de inventory van speler of NPC**, nooit direct vanuit een shelf. **Pas in de latere legale shop** kun je **employees** inhuren die dit pak/verdeel-werk automatisch doen. Huidige code voldoet hier al aan (shelf = Store/Take naar inventory; geen verkoop vanaf shelf).

- Perspectief: first-person, PC (muis/toetsenbord) — speler staat zelf achter de toonbank/deur.
- Structuur: endless met milestones; milestones unlocken producten/gear en sturen fase-overgangen.
- 3 fases: straatdealer (appartement, = MVP) → legale winkel → franchise.
- Voorraad: zelf kweken (geen inkoop); planten groeien real-time, oogst = voorraad.
- Werving: gratis joint-samples op straat verhogen verslaving/respect/loyaliteit → prospect komt naar appartement.
- Deal-mechaniek: prijs-slider t.o.v. markt; live acceptatie-% uit prijs + respect/loyaliteit/verslaving; afdingen → boos weglopen.
- Klanten: een paar archetypes; wachtrij + geduld-timer.
- Dag/nacht: real-time 20 min licht / 10 min donker, doorlopend (geen "volgende dag"). Nacht = meer/schichtigere klanten, hoger politierisico (heat → lichte bust), overval-risico.
- Upgrades: kweek-gear, opslag/voorraad, beveiliging, pand/personeel.
- Fase 1 → 2: geld-drempel + vergunning kopen.
- Verhaal: licht (intro + milestone-beats). Onboarding: leren door te doen, geen tutorial.
- Art: stylized/cartoon, Amsterdamse coffeeshop-vibe. Audio: chill/lo-fi/reggae.
- Fail-state: busted-risico licht, failliet mogelijk, geen harde game-over.
- Scope: kleinst mogelijke eerste versie (alleen kern-loop), daarna uitbouwen.
- Tech: C++ voor logica + Blueprint voor koppeling; C++ state machine voor klant-AI (geen Behaviour Tree).

## Bouw-log (gaandeweg)

- [2026-05-30] Engine-versie vastgepind op **Unreal Engine 5.7** — project aangemaakt vanuit de **First-Person C++**-template (heet `ThePlugSIM`). Stap B + C uit het A–Z-plan klaar.
- [2026-05-30] **Co-op vanaf dag 1, solo speelbaar.** De game moet in je eentje volledig speelbaar zijn, maar **online co-op (2-3 spelers)** moet erin komen. Daarom is élke feature **replication-aware**: server-authoritative state, mutaties via Server-RPC of op de server, visuals/UI lokaal. Vuistregel per nieuw systeem: bedenk vooraf wie de authority is, wat repliceert (`Replicated`/`RepNotify` + `OnRep_`-delegates), en welke acties een Server-RPC nodig hebben. Subsystems voor gedeelde staat (geld, voorraad) draaien server-authoritative; per-speler-data hangt aan de (replicerende) pawn/PlayerState. (Vervangt de eerdere single-player-only aanname.)
- [2026-05-30] Tweede C++-module **`WeedShopCore`** aangemaakt (Source/WeedShopCore, leeg + compileert). Alle eigen gameplay komt hierin; de template-module `ThePlugSIM` blijft boilerplate. Stap E uit het A–Z-plan. Module-naam volgt de brief (`WeedShopCore`), niet de projectnaam.
- [2026-05-30] **Roll-UI + joint-kwaliteit**: **R** opent een roll-paneel (klikbare grams-keuze 1–5 + kwaliteit-balk + "Draai"-knop). `ServerRollJoint(grams)` verbruikt dat aantal gram bud → maakt een `Joint_<G>g` (kwaliteit zit in de id). **Sample (F)** pakt de beste joint; effect (loyaliteit/verslaving/respect) **schaalt met kwaliteit**; **kieskeurige/weinig-verslaafde NPC's (locals)** weigeren een slappe joint (<50%) → loyaliteit omlaag. Roll-logica/-state in `UPhoneClientComponent`; HUD tekent het klikbare paneel.
- [2026-05-30] **Joint draaien + hotbar + co-op spawn-fix + interactie-cooldown**: **R** draait een joint (1 gram `Bud_*` → 1 `Joint`, server-authoritative); **sample (F) verbruikt nu een `Joint`** i.p.v. ruwe bud (eerst draaien). HUD heeft een **hotbar** onderaan (voorraad-slots + R/F-hint). `TryInteract` kreeg een **cooldown** (0.5s) zodat E-ingedrukt-houden niet spamt. `Map_Apartment` kreeg **3 PlayerStarts** (was 1) zodat co-op-speler 2/3 boven de vloer spawnen i.p.v. onder de map.
- [2026-05-30] **Risico-laag + straat-werving + 100 NPC's**: `UHeatComponent` op de GameState (heat ↑ 's nachts/bij risico, ↓ overdag; hoge heat 's nachts → lichte **bust** of **overval** = cash-verlies; `HeatResist`-upgrade dempt; HUD toont Heat%). **Straat-werving**: toets **F** op een aangekeken NPC → gratis gram → respect/loyaliteit/verslaving van die persoon omhoog in het register (richting z'n nummer) + beetje heat. Server-authoritative (`ServerGiveSample`). `DT_NPCs` opgeschaald naar **100** gegenereerde personen (CSV, schaalbaar).
- [2026-05-30] **NPC-roster + nummer-unlock** (gevraagd, richting ~100 NPC's): `DT_NPCs` (genaamde personen + begin-stats; nu 20, schaalt door rijen) + `UNpcRegistryComponent` op de GameState met **persistente per-persoon stats** (respect/loyaliteit/verslaving), `AssignNpc` round-robin. `ACustomerBase` heeft een `NpcId`: laadt stats bij spawn, schrijft terug bij deals → leveling per persoon. **Contact ('nummer') unlockt pas bij loyaliteit ≥ `UnlockLoyalty` (40)**, niet meer bij eerste contact; register registreert 'm in ContactsComponent + melding. Afspraak-klanten spawnen als hun échte contact-NPC (deferred spawn zodat stats laden). Contact-id's = NpcId's nu.
- [2026-05-30] **Git + Git LFS** geïnitialiseerd. `.gitignore` (Binaries/Intermediate/Saved/DDC etc.) + `.gitattributes` (LFS voor *.uasset/*.umap/*.fbx/*.png/*.wav etc.). Stap D. Remote + push moet de gebruiker nog doen.
- [2026-05-30] **Interaction-systeem** (stap I): `IInteractable` (interface, methodes `Interact` + `GetInteractionPrompt` als BlueprintNativeEvent) + `UInteractionComponent` (camera line-trace, focus-tracking, `OnFocusedInteractableChanged`-delegate voor prompt-UI, `TryInteract()` voor de input). Let op: in UE 5.7 genereert UHT zelf de default-body van interface-BlueprintNativeEvents — dus géén `_Implementation` in de .cpp zetten. `CanInteract` bewust weggelaten (auto-default zou `false` zijn); gating gebeurt binnen `Interact`.
- [2026-05-30] **Starter-wereld `Map_Apartment`** (gevraagd): greybox studentenkamer (6×5m, muren/plafond, badkamer/grow-hoek met deuropening, meubel-boxes) headless via Python gebouwd (StaticMeshActor + engine-cube). DirectionalLight + SkyAtmosphere + SkyLight. PlayerStart + 2 lege potten (grow x20) + 2 koopstations + geld-test-kubus + 1 klant erin. GameMode-override = WeedShopGameMode. Default-map in `DefaultEngine.ini` (Editor + Game). Vervangt de template-testruimte. (Te polijsten: art/meubels; SkyLight real-time-capture nog goed zetten.)
- [2026-05-30] **Klikbare telefoon (knoppen + hover)** (gevraagd): telefoon-logica naar `UPhoneClientComponent` op de pawn (decoupled van de template-character; HUD kan 't aanroepen zonder module-cykel). `AWeedShopHUD` tekent nu **klikbare knoppen** via hit-boxes (tabs, koop/actie-rijen, sluiten) met **hover-highlight + tooltip-regel**; cijfertoetsen 1-6 blijven als reserve. PC krijgt `bEnableClickEvents`/`bEnableMouseOverEvents` + cursor bij open. Bagseed-zaadprijs 0 → €10 (0 brak het kopen). Let op: hit-box-klikken is functioneel maar niet visueel door mij geverifieerd.
- [2026-05-30] **Afspraak-klant arriveert**: een **geaccepteerde** afspraak laat op z'n tijd een `ACustomerBase` spawnen vóór de speler (wil Bud_NorthernLights, neemt de contact-relatie over als loyaliteit/respect) — gewone deal-flow erop. Klanten despawnen ~12s na geholpen/vertrokken. Afspraak-loop rond: bericht → accepteren → klant komt op tijd → verkopen.
- [2026-05-30] **Klok + afspraak accepteren/weigeren**: HUD toont nu een **in-game klok** (Tijd: HH:MM) uit de dag-cyclus. Berichten kregen `Status` (open/ja/nee); in de Berichten-tab accepteer je het eerste open bericht met **1** of weiger je met **2**. Accepteren = relatie + loyaliteit **+5**, weigeren **−12** (op zowel het contact als de live `ACustomerBase`). Alleen **geaccepteerde** afspraken worden op hun tijd aangekondigd. (Vervolg: NPC echt laten arriveren/spawnen op de afspraaktijd.)
- [2026-05-30] **Contacten + berichten (telefoon v3)** (gevraagd): `UContactsComponent` op de GameState (replicated). Klanten worden **contact** bij eerste interactie (`ACustomerBase` → `RegisterContact`, naam uit een pool, relatie = gem. respect/loyaliteit/verslaving). Server-timer stuurt periodiek (~25s) een **afspraak-bericht** van een willekeurig contact: "Ik kom om HH:MM langs" of "Kom om HH:MM bij mij" (tijd uit de dag-cyclus). Bij de afspraaktijd volgt een on-screen **melding**. Telefoon heeft nu **4 tabs** (Q cyclet: Upgrades/Suppliers/Contacten/Berichten). v1: berichten worden getoond + aangekondigd; het daadwerkelijk laten arriveren/spawnen van de NPC op de afspraaktijd is een vervolgstap.
- [2026-05-30] **Supplier + planten (telefoon v2)**: `UStoreComponent` op de GameState verkoopt **zaden** uit DT_Strains (`BuySeed` → kas eraf + `Seed_<strain>` in inventory; `SeedItemId`/`StrainFromSeedItem`). Telefoon kreeg een **tab-systeem** (Q wisselt Upgrades ↔ Suppliers; 1–6 koopt in actieve tab) + `ServerBuySeed`-RPC. `AGrowPlant` heeft nu een **lege-pot-staat** (`bPlanted`): lege pot + zaad in inventory → planten (verbruikt zaad, start groei); oogst maakt de pot weer leeg (herbruikbaar i.p.v. verwijderen); vooraf-geplaatste plant telt als geplant. Twee lege potten in het level geplaatst. Volledige speler-loop: zaad kopen → planten → groeien → oogsten → verkopen.
- [2026-05-30] **Telefoon-UI v1** (gevraagd door user): functioneel, code-only (geen UMG) — `AWeedShopHUD::DrawPhone` tekent een paneel met de upgrade-catalogus (genummerd 1–6, naam + prijs + gekocht/vergrendeld). Toggle met **Tab**; nummertoetsen 1–6 kopen. Op de character via directe `BindKey` (geen Input Action-assets nodig) → `TogglePhone` (zet muis-cursor + InputMode) en `BuyPhoneKey`→`BuyPhoneIndex`→`ServerBuyUpgrade`-RPC (server-authoritative). Catalogus uit DT_Upgrades incl. appartement-rijen (Apt_Renovate, Apt_BuyShop). Een nette UMG-skin kan dit later vervangen.
- [2026-05-30] **Upgrades** (stap S, tweede helft): `FUpgradeRow`/`DT_Upgrades` (categorie, kosten, vereiste fase, **EffectTag + magnitude**). `UUpgradeComponent` op de GameState (replicated): `BuyUpgrade` (server: fase-check → `Economy::RemoveMoney` → markeer gekocht), `IsPurchased`, `GetEffectTotal(tag)`. Effecten data-driven opgeteld: `AGrowPlant` leest `GrowthSpeed` (sneller groeien) en `CareRetention` (minder droogte-stress). `AUpgradeStation` (interactable) koopt een ingestelde upgrade. CSV-rijen: betere pot, voeding, LED-lamp, beveiliging. Twee stations (LED + pot) in het level geplaatst.
- [2026-05-30] **Milestones/progressie** (stap S): `UMilestoneComponent` op de GameState (server-authoritative, replicated). Telt **totaal-verdiend** mee via nieuwe `EconomyComponent::OnMoneyEarned` (alleen inkomsten). `FMilestoneRow`/`DT_Milestones` (drempel verdiend → unlock product + fase). `EShopPhase` (Straatdealer/Winkel/Franchise). Bij drempel: `OnMilestoneReached` + on-screen melding; fase stijgt → `OnShopPhaseChanged`. `IsProductUnlocked()` voor latere product-gating. HUD toont fase + totaal verdiend. Milestone-tabel via ConstructorHelpers (`/Game/_Project/Data/DT_Milestones`). Upgrades (DT_Upgrades + kopen) = volgende sub-stap.
- [2026-05-30] **Speelbare slice autonoom ingericht** (zonder editor-handwerk): speler-character kreeg `UInventoryComponent` in C++ (ThePlugSIM hangt nu van WeedShopCore). `AWeedShopGameMode` hergebruikt BP_FirstPersonCharacter/Controller en zet GameState + `AWeedShopHUD`; project-default GameMode in `DefaultEngine.ini` → `WeedShopGameMode`. `AWeedShopHUD` = C++ canvas-overlay (kas/dag/voorraad/prompt, geen UMG). Via headless Python (`-run=pythonscript`): `DT_Products`/`DT_Strains` geïmporteerd naar `/Game/_Project/Data`, en een NorthernLights-`AGrowPlant` + een `ACustomerBase` (wil Bud_NorthernLights) bij de PlayerStart geplaatst + map opgeslagen. Bekende ruwe randen: klant-body nog onzichtbaar (geen skeletal mesh, wél interactbaar); plant groeit ~5 min real-time; geen NavMesh/UMG.
- [2026-05-30] **Save/Load** (stap P): `UWeedShopSaveGame` (USaveGame, uitbreidbaar: nu kas + dag-tijd) + `USaveGameSubsystem` (GameInstance-subsystem) met `SaveGame`/`LoadGame`/`HasSave`. Alleen host/server schrijft + herstelt de gedeelde state (clients via replicatie). Restore-setters `EconomyComponent::SetBalanceCents` + `DayCycleComponent::SetTimeOfDaySeconds` (server-only) toegevoegd. Voorraad/planten/klanten in save = later.
- [2026-05-30] **Dag/nacht** (stap N): `UDayCycleComponent` op de GameState (niet als WorldSubsystem — subsystems repliceren niet; zelfde redenering als de kas). Doorlopende real-time klok **20 min licht / 10 min donker**; server telt op + repliceert `TimeOfDaySeconds`, clients lezen. `IsNight()`, `GetCycleFraction()` (0–1, voor zon-rotatie), `OnDayNightChanged`-delegate (server + clients) voor ambient/nacht-effecten. Lighting koppelen = editor.
- [2026-05-30] **Deal + Customer** (stappen L + M): `UWeedDealLibrary::CalculateAcceptanceChance` = de tunebare formule uit de brief (prijs/markt + respect×0.15 + loyaliteit×0.15 + verslaving×0.25, clamp 0–100), pure functie voor zowel UI-slider als klant-AI. `EDealResult` (Accepted/Haggle/Refused/NoStock). `FCustomerTypeRow` (DT_CustomerTypes: voorkeur, geduld, budget, dag/nacht-spawn-gewicht, start-attributen). `ACustomerBase : ACharacter` met **C++ state-machine** (WantsToOrder→Negotiating→Served/Leaving), replicated respect/loyaliteit/verslaving + geduld-timer (op 0 → boos weg, −respect). `SubmitOffer` (server): voorraad-check → budget-check → kans-roll → betaal naar kas + voorraad eraf + attributen bij (eerlijke deal +respect/+loyaliteit, uitknijpen −; elke koop +verslaving). Capsule blokkeert Visibility zodat de interactie-trace raakt. `Interact` = snel-test: verkoopt tegen marktprijs uit speler-inventory naar de gedeelde kas. Vormt samen met plant/oogst/inventory/economy een werkende mini-loop. (NavMesh/spawn/wachtrij + prijs-slider-UI = later, editor.)
- [2026-05-30] **Conventions** (stap F): `Docs/CONVENTIONS.md` toegevoegd (modules, Source/Content-mapstructuur, naamprefixes, geld-in-cents, co-op-regel, BP-exposure, één-systeem-per-commit).
- [2026-05-30] **Inventory** (stap K): `UInventoryComponent` (replicated, server-authoritative) met `TArray<FInventoryStack>` (item-id + aantal), `AddItem`/`RemoveItem` server-only, `OnInventoryChanged`-delegate, optionele `MaxStacks`. Item-id's = rij-namen uit DT_Products / DT_Strains-opbrengst. Op pawn (persoonlijk) of opslag-actor (gedeeld).
- [2026-05-30] **Cultivation** (stap K / Fase 4): `FWeedStrainRow` (DT_Strains: BaseThc%, BaseYieldGrams, GrowMinutes real-time, SeedPrice, HarvestProductId) + `Data/DT_Strains.csv` (Bagseed/NorthernLights/GranddaddyPurple/SourDiesel/OGKush). `AGrowPlant` (replicated): groeit op echte tijd door 5 fases (Seedling→…→Harvestable), `CareMultiplier` 0.3–1.0 (water verhoogt, droogte-stress verlaagt langzaam, geen permanente dood), IInteractable → water geven of oogsten. Oogst: `yield = BaseYield×care`, `thc = BaseThc×care×noise(0.9–1.1)`, gram → InventoryComponent van de oogster, daarna plant verwijderd. `PhaseMeshes` per fase via OnRep_Visual.
- [2026-05-30] **GameMode + test-loop**: `AWeedShopGameMode : AGameModeBase` zet `GameStateClass = AWeedShopGameState` (pawn/PC/HUD via BP-subclass of reparent, zodat de FP-pawn behouden blijft). `AMoneyTestPickup` (IInteractable, engine-kubus, `bReplicates`) → interact voegt server-authoritative €10 toe aan de kas — smoke-test voor de keten interactie → ServerInteract-RPC → replicated saldo. Verwijderen zodra echte deals bestaan.
- [2026-05-30] **DT_Products** (stap K, data-fundering): `FWeedShopProductRow : FTableRowBase` (`Source/WeedShopCore/Public/Data/WeedShopProduct.h`) met `DisplayName`, `Category` (enum `EProductCategory`: Weed/Edible/Accessory), `MarketPriceCents` (int32, cents), `Popularity` (0..1), `UnlockMilestone` (FName, None = direct verkoopbaar), soft `WorldMesh`/`Icon`. Start-CSV `Data/DT_Products.csv` met 3 dummy's (Northern Lights-pot €40, joint €7, Space Cake €12 achter milestone). CSV is een source-artifact in `Data/`; importeren als DataTable naar `/Content/_Project/Data/DT_Products` (Row Type = WeedShopProductRow).
- [2026-05-30] **Economy** (stap J): geld leeft als **gedeelde co-op-kas op de GameState**, niet in een GameInstanceSubsystem (die kan niet repliceren — bewuste afwijking van de brief). `UEconomyComponent` (replicated, server-authoritative) houdt het saldo in **eurocents** (`int64`, geen float-money) met `ReplicatedUsing=OnRep_Balance` → `OnBalanceChanged`-delegate voor de UI. `AddMoney`/`RemoveMoney` werken alleen op de server; `RemoveMoney` faalt bij onvoldoende saldo. Startsaldo €50,00 (5000 cents). Gehost door `AWeedShopGameState` (zet als Game State Class op de GameMode). Cross-map behoud loopt later via save/load, niet via het component.
- [2026-05-30] **Interaction is co-op-correct** (eerste toepassing van de replication-regel): component repliceert; focus/prompt-trace draait alleen lokaal (`IsLocallyControlled`); `TryInteract` voert direct uit bij authority (host/SP) of stuurt anders een **Server-RPC** `ServerInteract(Target)` die op de server valideert (geldig, IInteractable, binnen bereik) en dan `Interact` uitvoert. Dit is het sjabloon voor alle latere interacties.

## Nog te beslissen

- Specifieke klant-archetypes.
- Of edibles van eigen oogst gemaakt worden (crafting) + accessoires als merch.
- Alle balans-getallen (tunen tijdens spelen).

## 2026-07-05 - HUD/UX polish beslissingen

- Controls-overlay uit verbergt alleen het rechteronder controls-lijstje; de center interact prompt blijft zichtbaar.
- Cash mag met Hold Q gedropt worden, maar alleen via een veilige economy/inventory-route zodat cash geen gratis geld of mirror-bug veroorzaakt.
- Toast stack max = 3 tegelijk; extra/oudste meldingen moeten rustig wegfaden of doorschuiven.
- Hotbar gameplay-slots blijven exact gecentreerd; het telefoon-icoon staat los rechts als sidecar en telt niet mee voor de hotbar-breedte.

## Woning-koopsysteem (Upgrades-app)
- 3 koopbare panden, deterministisch gekozen door CityGenerator::GetPropertyOffers:
  starter klein flatje (hoogste verdieping, gratis/al van jou), rijtjeshuis (EUR 15.000),
  grote flat-kamer (begane grond, EUR 40.000). Betaald uit BANK-saldo.
- Eigendom per speler op PhoneClientComponent (Replicated OwnedHomes + ActiveHome),
  opgeslagen in FPlayerSaveData. Verse start kent automatisch het starter-flatje toe
  + zet de speler erin; load herstelt de staat.
- De 3 panden blijven altijd vrij (CustomerSpawner slaat ze over; deur = "TE KOOP").
  Gekochte woning ontgrendelt lokaal als "Your home" (CityDoor::SetPlayerHome).

## LAN co-op (host/join via hoofdmenu)
- Hoofdmenu krijgt een CO-OP (LAN)-knop -> kaart met "Host nieuw co-op spel" + een
  IP-veld met "Join". Geen Steam/EOS nodig; gebruikt de standaard IP-netdriver (poort 7777).
- Host: SaveGameSubsystem::HostNewGameLan -> verse start + ReloadCurrentLevel("listen")
  zodat het level als listen-server herlaadt (RunPendingOnWorldReady draait op de host).
- Join: SaveGameSubsystem::JoinLan -> PlayerController::ClientTravel(IP[:7777], absolute).
- Clients (NM_Client) slaan het hoofdmenu over; host herstelt/repliceert speler-state.
- Firewall: sta de exe toe (UDP 7777). Vriend heeft een packaged build nodig.

## Bezorging + wereld-fixtures
- Pakket-deliveries landen nu voor de voordeur van JE WONING (FApartmentHome.DoorPos =
  begane-grond ingang; voor flats de gebouw-ingang) i.p.v. de shop-spawner.
- Verse game: server (CustomerSpawner) plaatst meubels (Table/Fridge/Mattress) in elke
  koopbare/starter-woning + een ATM in elke winkel. Server-spawned APlaceableProp/AAtm
  -> repliceert, oppakbaar/verplaatsbaar, en gaat mee in de save. Bij LOAD niet opnieuw
  (SaveGameSubsystem::IsFreshGame guard; de save herstelt ze zelf).
- ATM niet meer in de starter-inventory (staat nu in elke winkel).

## 2026-06-04 — Big-list batch 2

- **Pot visueel**: PotInner (donkere verzonken schijf, altijd zichtbaar) -> lege pot oogt hol; grotere/hogere SoilMesh bedekt 'm zodra soil is toegevoegd. SoilId repliceerde al.
- **Lift cabine**: runtime-componenten (wanden/deuren) worden nu expliciet via AttachToComponent(Root, KeepRelativeTransform) NA RegisterComponent vastgemaakt; SetupAttachment alleen liet ze in de wereld staan terwijl alleen de platform-vloer meeging.
- **NPC-afspraken** (ContactsComponent + CustomerBase): een afspraak stuurt de BESTAANDE bewoner met dat NpcId aan i.p.v. een dubbele NPC te spawnen.
  - YouGoToThem ("kom bij mij"): NPC verschijnt op de afspraaktijd in z'n eigen unit (HomeInteriorPos, boven) en wacht; bericht vermeldt het huisnummer.
  - TheyComeToYou ("ik kom langs"): NPC wacht bij de hoofddeur van z'n eigen gebouw (HomeFrontSpot, begane grond).
  - Afspraak heeft voorrang op roam/nacht; eindigt na de deal of na 6 min timeout; BecomeBuyerNow bij start.
- **Roamers/nacht**: MaxResidents=40, gespreid over alle huizen; 's nachts naar huis + binnen verdwijnen — al aanwezig.
- **OPEN**: #3 NPC's alleen op de stoep (oversteken alleen naar de volgende stoep) — vereist nav-area kosten op de wegen; nog niet gedaan.

## 2026-06-06 — Furniture-templates + sandbox authoring

- **FurnitureTemplateLib**: meubel-layout per woning-type (sleutel = soort + gequantiseerde kamermaat, gesorteerd zodat oriëntatie samenvalt). Opslag in een canoniek frame (lange muur = lokale X); bij plaatsen draait de layout mee naar de oriëntatie van elk huis + clamp binnen RoomHalf.
- **Authoring**: sandbox (free-build = overal plaatsen) + hotkeys F8 (opslaan), F9 (wissen), F10 (types-overzicht). Capture neemt per type de woning met de meeste meubels; ruime woning-detectie (160cm + nearest-fallback) zodat alles meegaat.
- **Default**: layout is INGEBAKKEN in code (4 types, elk met 1 sink) -> werkt altijd; Saved/FurnitureTemplates.txt (F8) overschrijft.
- **Toepassing**: offer-woningen = interactieve meubels; NPC-woningen = cosmetisch (tag "Cosmetic", niet oppakbaar). 
- **Sink = vaste fixture**: ALTIJD cosmetisch/locked (ook eigen woning), uit grants gehaald, plaatsing geblokkeerd in ServerPlace. AWaterSink spawnt met AlwaysSpawn (anders geweigerd bij botsing).
- **Grote kamer**: begane-grond appartement-units NApt-1 per zijde = breder (eigen groter type Apt_7x11). Scheidingswanden (SegS) ook op NAptF/AptLenF i.p.v. NApt/AptLen (anders muur dwars door de kamer).
- **Sandbox-inventory**: ClearAll + nette set (standaard starter + plaatsbare meubels), geen bulk/alle-zaden meer.
- **Bekende capture-bug (open)**: FindHomeForPoint kan bij gestapelde appartementen de verkeerde verdieping kiezen (Z-offset ~1 etage). Fix = 3D-afstand i.p.v. XY-only; nog niet doorgevoerd.
