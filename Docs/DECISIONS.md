# Decisions Log

Elke architectuur-, ontwerp- of balans-beslissing hier in 1-2 regels bijschrijven, met datum. Dit is het geheugen van het project — de AI-agent leest dit om consistent te blijven over sessies heen.

> Formaat: `- [JJJJ-MM-DD] Beslissing — korte reden.`

## Vastgelegd (uit de brief, v2)

- Perspectief: first-person, PC (muis/toetsenbord) — speler staat zelf achter de toonbank/deur.
- Structuur: endless met milestones; milestones unlocken producten/gear en sturen fase-overgangen.
- 3 fases: straatdealer (appartement, = MVP) → legale winkel → franchise.
- Voorraad: zelf kweken (geen inkoop); planten groeien real-time, oogst = voorraad.
- Werving: gratis joint-samples op straat verhogen verslaving/respect/loyaliteit → prospect komt naar appartement.
- Deal-mechaniek: prijs-slider t.o.v. markt; live acceptatie-% uit prijs + respect/loyaliteit/verslaving; afdingen → boos weglopen.
- Klanten: een paar archetypes; wachtrij + geduld-timer.
- Dag/nacht: real-time 20 min licht / 10 min donker, doorlopend (geen "volgende dag"). Nacht = meer/schichtigere klanten, hoger politierisico (heat → lichte bust), overval-risico.
- Upgrades: kweek-gear, opslag/voorraad, beveiliging, pand/personeel.
- Fase 1 → 2: geld-drempel + vergunning kopen.
- Verhaal: licht (intro + milestone-beats). Onboarding: leren door te doen, geen tutorial.
- Art: stylized/cartoon, Amsterdamse coffeeshop-vibe. Audio: chill/lo-fi/reggae.
- Fail-state: busted-risico licht, failliet mogelijk, geen harde game-over.
- Scope: kleinst mogelijke eerste versie (alleen kern-loop), daarna uitbouwen.
- Tech: C++ voor logica + Blueprint voor koppeling; C++ state machine voor klant-AI (geen Behaviour Tree).

## Bouw-log (gaandeweg)

- [2026-05-30] Engine-versie vastgepind op **Unreal Engine 5.7** — project aangemaakt vanuit de **First-Person C++**-template (heet `ThePlugSIM`). Stap B + C uit het A–Z-plan klaar.
- [2026-05-30] **Co-op vanaf dag 1, solo speelbaar.** De game moet in je eentje volledig speelbaar zijn, maar **online co-op (2-3 spelers)** moet erin komen. Daarom is élke feature **replication-aware**: server-authoritative state, mutaties via Server-RPC of op de server, visuals/UI lokaal. Vuistregel per nieuw systeem: bedenk vooraf wie de authority is, wat repliceert (`Replicated`/`RepNotify` + `OnRep_`-delegates), en welke acties een Server-RPC nodig hebben. Subsystems voor gedeelde staat (geld, voorraad) draaien server-authoritative; per-speler-data hangt aan de (replicerende) pawn/PlayerState. (Vervangt de eerdere single-player-only aanname.)
- [2026-05-30] Tweede C++-module **`WeedShopCore`** aangemaakt (Source/WeedShopCore, leeg + compileert). Alle eigen gameplay komt hierin; de template-module `ThePlugSIM` blijft boilerplate. Stap E uit het A–Z-plan. Module-naam volgt de brief (`WeedShopCore`), niet de projectnaam.
- [2026-05-30] **NPC-roster + nummer-unlock** (gevraagd, richting ~100 NPC's): `DT_NPCs` (genaamde personen + begin-stats; nu 20, schaalt door rijen) + `UNpcRegistryComponent` op de GameState met **persistente per-persoon stats** (respect/loyaliteit/verslaving), `AssignNpc` round-robin. `ACustomerBase` heeft een `NpcId`: laadt stats bij spawn, schrijft terug bij deals → leveling per persoon. **Contact ('nummer') unlockt pas bij loyaliteit ≥ `UnlockLoyalty` (40)**, niet meer bij eerste contact; register registreert 'm in ContactsComponent + melding. Afspraak-klanten spawnen als hun échte contact-NPC (deferred spawn zodat stats laden). Contact-id's = NpcId's nu.
- [2026-05-30] **Git + Git LFS** geïnitialiseerd. `.gitignore` (Binaries/Intermediate/Saved/DDC etc.) + `.gitattributes` (LFS voor *.uasset/*.umap/*.fbx/*.png/*.wav etc.). Stap D. Remote + push moet de gebruiker nog doen.
- [2026-05-30] **Interaction-systeem** (stap I): `IInteractable` (interface, methodes `Interact` + `GetInteractionPrompt` als BlueprintNativeEvent) + `UInteractionComponent` (camera line-trace, focus-tracking, `OnFocusedInteractableChanged`-delegate voor prompt-UI, `TryInteract()` voor de input). Let op: in UE 5.7 genereert UHT zelf de default-body van interface-BlueprintNativeEvents — dus géén `_Implementation` in de .cpp zetten. `CanInteract` bewust weggelaten (auto-default zou `false` zijn); gating gebeurt binnen `Interact`.
- [2026-05-30] **Starter-wereld `Map_Apartment`** (gevraagd): greybox studentenkamer (6×5m, muren/plafond, badkamer/grow-hoek met deuropening, meubel-boxes) headless via Python gebouwd (StaticMeshActor + engine-cube). DirectionalLight + SkyAtmosphere + SkyLight. PlayerStart + 2 lege potten (grow x20) + 2 koopstations + geld-test-kubus + 1 klant erin. GameMode-override = WeedShopGameMode. Default-map in `DefaultEngine.ini` (Editor + Game). Vervangt de template-testruimte. (Te polijsten: art/meubels; SkyLight real-time-capture nog goed zetten.)
- [2026-05-30] **Klikbare telefoon (knoppen + hover)** (gevraagd): telefoon-logica naar `UPhoneClientComponent` op de pawn (decoupled van de template-character; HUD kan 't aanroepen zonder module-cykel). `AWeedShopHUD` tekent nu **klikbare knoppen** via hit-boxes (tabs, koop/actie-rijen, sluiten) met **hover-highlight + tooltip-regel**; cijfertoetsen 1-6 blijven als reserve. PC krijgt `bEnableClickEvents`/`bEnableMouseOverEvents` + cursor bij open. Bagseed-zaadprijs 0 → €10 (0 brak het kopen). Let op: hit-box-klikken is functioneel maar niet visueel door mij geverifieerd.
- [2026-05-30] **Afspraak-klant arriveert**: een **geaccepteerde** afspraak laat op z'n tijd een `ACustomerBase` spawnen vóór de speler (wil Bud_NorthernLights, neemt de contact-relatie over als loyaliteit/respect) — gewone deal-flow erop. Klanten despawnen ~12s na geholpen/vertrokken. Afspraak-loop rond: bericht → accepteren → klant komt op tijd → verkopen.
- [2026-05-30] **Klok + afspraak accepteren/weigeren**: HUD toont nu een **in-game klok** (Tijd: HH:MM) uit de dag-cyclus. Berichten kregen `Status` (open/ja/nee); in de Berichten-tab accepteer je het eerste open bericht met **1** of weiger je met **2**. Accepteren = relatie + loyaliteit **+5**, weigeren **−12** (op zowel het contact als de live `ACustomerBase`). Alleen **geaccepteerde** afspraken worden op hun tijd aangekondigd. (Vervolg: NPC echt laten arriveren/spawnen op de afspraaktijd.)
- [2026-05-30] **Contacten + berichten (telefoon v3)** (gevraagd): `UContactsComponent` op de GameState (replicated). Klanten worden **contact** bij eerste interactie (`ACustomerBase` → `RegisterContact`, naam uit een pool, relatie = gem. respect/loyaliteit/verslaving). Server-timer stuurt periodiek (~25s) een **afspraak-bericht** van een willekeurig contact: "Ik kom om HH:MM langs" of "Kom om HH:MM bij mij" (tijd uit de dag-cyclus). Bij de afspraaktijd volgt een on-screen **melding**. Telefoon heeft nu **4 tabs** (Q cyclet: Upgrades/Suppliers/Contacten/Berichten). v1: berichten worden getoond + aangekondigd; het daadwerkelijk laten arriveren/spawnen van de NPC op de afspraaktijd is een vervolgstap.
- [2026-05-30] **Supplier + planten (telefoon v2)**: `UStoreComponent` op de GameState verkoopt **zaden** uit DT_Strains (`BuySeed` → kas eraf + `Seed_<strain>` in inventory; `SeedItemId`/`StrainFromSeedItem`). Telefoon kreeg een **tab-systeem** (Q wisselt Upgrades ↔ Suppliers; 1–6 koopt in actieve tab) + `ServerBuySeed`-RPC. `AGrowPlant` heeft nu een **lege-pot-staat** (`bPlanted`): lege pot + zaad in inventory → planten (verbruikt zaad, start groei); oogst maakt de pot weer leeg (herbruikbaar i.p.v. verwijderen); vooraf-geplaatste plant telt als geplant. Twee lege potten in het level geplaatst. Volledige speler-loop: zaad kopen → planten → groeien → oogsten → verkopen.
- [2026-05-30] **Telefoon-UI v1** (gevraagd door user): functioneel, code-only (geen UMG) — `AWeedShopHUD::DrawPhone` tekent een paneel met de upgrade-catalogus (genummerd 1–6, naam + prijs + gekocht/vergrendeld). Toggle met **Tab**; nummertoetsen 1–6 kopen. Op de character via directe `BindKey` (geen Input Action-assets nodig) → `TogglePhone` (zet muis-cursor + InputMode) en `BuyPhoneKey`→`BuyPhoneIndex`→`ServerBuyUpgrade`-RPC (server-authoritative). Catalogus uit DT_Upgrades incl. appartement-rijen (Apt_Renovate, Apt_BuyShop). Een nette UMG-skin kan dit later vervangen.
- [2026-05-30] **Upgrades** (stap S, tweede helft): `FUpgradeRow`/`DT_Upgrades` (categorie, kosten, vereiste fase, **EffectTag + magnitude**). `UUpgradeComponent` op de GameState (replicated): `BuyUpgrade` (server: fase-check → `Economy::RemoveMoney` → markeer gekocht), `IsPurchased`, `GetEffectTotal(tag)`. Effecten data-driven opgeteld: `AGrowPlant` leest `GrowthSpeed` (sneller groeien) en `CareRetention` (minder droogte-stress). `AUpgradeStation` (interactable) koopt een ingestelde upgrade. CSV-rijen: betere pot, voeding, LED-lamp, beveiliging. Twee stations (LED + pot) in het level geplaatst.
- [2026-05-30] **Milestones/progressie** (stap S): `UMilestoneComponent` op de GameState (server-authoritative, replicated). Telt **totaal-verdiend** mee via nieuwe `EconomyComponent::OnMoneyEarned` (alleen inkomsten). `FMilestoneRow`/`DT_Milestones` (drempel verdiend → unlock product + fase). `EShopPhase` (Straatdealer/Winkel/Franchise). Bij drempel: `OnMilestoneReached` + on-screen melding; fase stijgt → `OnShopPhaseChanged`. `IsProductUnlocked()` voor latere product-gating. HUD toont fase + totaal verdiend. Milestone-tabel via ConstructorHelpers (`/Game/_Project/Data/DT_Milestones`). Upgrades (DT_Upgrades + kopen) = volgende sub-stap.
- [2026-05-30] **Speelbare slice autonoom ingericht** (zonder editor-handwerk): speler-character kreeg `UInventoryComponent` in C++ (ThePlugSIM hangt nu van WeedShopCore). `AWeedShopGameMode` hergebruikt BP_FirstPersonCharacter/Controller en zet GameState + `AWeedShopHUD`; project-default GameMode in `DefaultEngine.ini` → `WeedShopGameMode`. `AWeedShopHUD` = C++ canvas-overlay (kas/dag/voorraad/prompt, geen UMG). Via headless Python (`-run=pythonscript`): `DT_Products`/`DT_Strains` geïmporteerd naar `/Game/_Project/Data`, en een NorthernLights-`AGrowPlant` + een `ACustomerBase` (wil Bud_NorthernLights) bij de PlayerStart geplaatst + map opgeslagen. Bekende ruwe randen: klant-body nog onzichtbaar (geen skeletal mesh, wél interactbaar); plant groeit ~5 min real-time; geen NavMesh/UMG.
- [2026-05-30] **Save/Load** (stap P): `UWeedShopSaveGame` (USaveGame, uitbreidbaar: nu kas + dag-tijd) + `USaveGameSubsystem` (GameInstance-subsystem) met `SaveGame`/`LoadGame`/`HasSave`. Alleen host/server schrijft + herstelt de gedeelde state (clients via replicatie). Restore-setters `EconomyComponent::SetBalanceCents` + `DayCycleComponent::SetTimeOfDaySeconds` (server-only) toegevoegd. Voorraad/planten/klanten in save = later.
- [2026-05-30] **Dag/nacht** (stap N): `UDayCycleComponent` op de GameState (niet als WorldSubsystem — subsystems repliceren niet; zelfde redenering als de kas). Doorlopende real-time klok **20 min licht / 10 min donker**; server telt op + repliceert `TimeOfDaySeconds`, clients lezen. `IsNight()`, `GetCycleFraction()` (0–1, voor zon-rotatie), `OnDayNightChanged`-delegate (server + clients) voor ambient/nacht-effecten. Lighting koppelen = editor.
- [2026-05-30] **Deal + Customer** (stappen L + M): `UWeedDealLibrary::CalculateAcceptanceChance` = de tunebare formule uit de brief (prijs/markt + respect×0.15 + loyaliteit×0.15 + verslaving×0.25, clamp 0–100), pure functie voor zowel UI-slider als klant-AI. `EDealResult` (Accepted/Haggle/Refused/NoStock). `FCustomerTypeRow` (DT_CustomerTypes: voorkeur, geduld, budget, dag/nacht-spawn-gewicht, start-attributen). `ACustomerBase : ACharacter` met **C++ state-machine** (WantsToOrder→Negotiating→Served/Leaving), replicated respect/loyaliteit/verslaving + geduld-timer (op 0 → boos weg, −respect). `SubmitOffer` (server): voorraad-check → budget-check → kans-roll → betaal naar kas + voorraad eraf + attributen bij (eerlijke deal +respect/+loyaliteit, uitknijpen −; elke koop +verslaving). Capsule blokkeert Visibility zodat de interactie-trace raakt. `Interact` = snel-test: verkoopt tegen marktprijs uit speler-inventory naar de gedeelde kas. Vormt samen met plant/oogst/inventory/economy een werkende mini-loop. (NavMesh/spawn/wachtrij + prijs-slider-UI = later, editor.)
- [2026-05-30] **Conventions** (stap F): `Docs/CONVENTIONS.md` toegevoegd (modules, Source/Content-mapstructuur, naamprefixes, geld-in-cents, co-op-regel, BP-exposure, één-systeem-per-commit).
- [2026-05-30] **Inventory** (stap K): `UInventoryComponent` (replicated, server-authoritative) met `TArray<FInventoryStack>` (item-id + aantal), `AddItem`/`RemoveItem` server-only, `OnInventoryChanged`-delegate, optionele `MaxStacks`. Item-id's = rij-namen uit DT_Products / DT_Strains-opbrengst. Op pawn (persoonlijk) of opslag-actor (gedeeld).
- [2026-05-30] **Cultivation** (stap K / Fase 4): `FWeedStrainRow` (DT_Strains: BaseThc%, BaseYieldGrams, GrowMinutes real-time, SeedPrice, HarvestProductId) + `Data/DT_Strains.csv` (Bagseed/NorthernLights/GranddaddyPurple/SourDiesel/OGKush). `AGrowPlant` (replicated): groeit op echte tijd door 5 fases (Seedling→…→Harvestable), `CareMultiplier` 0.3–1.0 (water verhoogt, droogte-stress verlaagt langzaam, geen permanente dood), IInteractable → water geven of oogsten. Oogst: `yield = BaseYield×care`, `thc = BaseThc×care×noise(0.9–1.1)`, gram → InventoryComponent van de oogster, daarna plant verwijderd. `PhaseMeshes` per fase via OnRep_Visual.
- [2026-05-30] **GameMode + test-loop**: `AWeedShopGameMode : AGameModeBase` zet `GameStateClass = AWeedShopGameState` (pawn/PC/HUD via BP-subclass of reparent, zodat de FP-pawn behouden blijft). `AMoneyTestPickup` (IInteractable, engine-kubus, `bReplicates`) → interact voegt server-authoritative €10 toe aan de kas — smoke-test voor de keten interactie → ServerInteract-RPC → replicated saldo. Verwijderen zodra echte deals bestaan.
- [2026-05-30] **DT_Products** (stap K, data-fundering): `FWeedShopProductRow : FTableRowBase` (`Source/WeedShopCore/Public/Data/WeedShopProduct.h`) met `DisplayName`, `Category` (enum `EProductCategory`: Weed/Edible/Accessory), `MarketPriceCents` (int32, cents), `Popularity` (0..1), `UnlockMilestone` (FName, None = direct verkoopbaar), soft `WorldMesh`/`Icon`. Start-CSV `Data/DT_Products.csv` met 3 dummy's (Northern Lights-pot €40, joint €7, Space Cake €12 achter milestone). CSV is een source-artifact in `Data/`; importeren als DataTable naar `/Content/_Project/Data/DT_Products` (Row Type = WeedShopProductRow).
- [2026-05-30] **Economy** (stap J): geld leeft als **gedeelde co-op-kas op de GameState**, niet in een GameInstanceSubsystem (die kan niet repliceren — bewuste afwijking van de brief). `UEconomyComponent` (replicated, server-authoritative) houdt het saldo in **eurocents** (`int64`, geen float-money) met `ReplicatedUsing=OnRep_Balance` → `OnBalanceChanged`-delegate voor de UI. `AddMoney`/`RemoveMoney` werken alleen op de server; `RemoveMoney` faalt bij onvoldoende saldo. Startsaldo €50,00 (5000 cents). Gehost door `AWeedShopGameState` (zet als Game State Class op de GameMode). Cross-map behoud loopt later via save/load, niet via het component.
- [2026-05-30] **Interaction is co-op-correct** (eerste toepassing van de replication-regel): component repliceert; focus/prompt-trace draait alleen lokaal (`IsLocallyControlled`); `TryInteract` voert direct uit bij authority (host/SP) of stuurt anders een **Server-RPC** `ServerInteract(Target)` die op de server valideert (geldig, IInteractable, binnen bereik) en dan `Interact` uitvoert. Dit is het sjabloon voor alle latere interacties.

## Nog te beslissen

- Specifieke klant-archetypes.
- Of edibles van eigen oogst gemaakt worden (crafting) + accessoires als merch.
- Alle balans-getallen (tunen tijdens spelen).
